# TUPLAS 
'''
Son solo de lectura por lo que una vez declaradas ya no pueden modificarse
'''

numbers = (1,2,3,4,5)
frutas = ("banana", "manzana", "uva")
print(f"Nuestra tupla -> {numbers}")
print(f"En la posicion 2 -> {frutas[1]}")

# Creando una lista de numeros
numbersList = list(numbers)
print(f"Copiando la tupla en una lista -> {numbersList}")